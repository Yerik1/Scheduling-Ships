//
// Created by user on 4/27/2026.
//

#ifndef TYPES_H
#define TYPES_H

typedef enum {LEFT, RIGHT} Direction;
typedef enum {NORMAL, FISHING, PATROL} ShipType;
typedef enum {READY, WAITING, RUNNING, BLOCKED, FINISHED} ShipState;

#endif //TYPES_H
